# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env

# Node
node_modules/
dist/
*.log

# Uploaded files (local dev). Both paths: the upload directory is relative to
# the working directory, so running pytest from the repo root writes to
# ./uploads rather than ./backend/uploads, and those artifacts were getting
# committed by accident.
backend/uploads/
/uploads/

# Secrets / local env
.env.prod
.env.*.local

# OS
.DS_Store
Thumbs.db

# Local-only planning docs (kept out of the repo)
docs/DESKTOP_CLIENT_PLAN.md
